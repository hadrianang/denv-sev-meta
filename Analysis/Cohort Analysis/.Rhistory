cholesky_factor_corr[2]: L_Rho_gr ~ lkj_corr_cholesky(2),
vector[2]: sigma_gr ~ exponential(1),
gq>matrix[2,2]:Rho_T <<- Chol_to_Corr(L_Rho_T),
gq>matrix[2,2] Rho_gr <<- Chol_to_Corr(L_Rho_gr)
W = rnorm(N)
bWG = 0.5
bWR = (-1)
giftsAB = rep(NA, N_dyads)
giftsBA = rep(NA, N_dyads)
lambda = log(c(0.5, 2))
for(i in 1:N_dyads){
A = dyads[i, 1]
B = dyads[i, 2]
giftsAB[i] = rpois(1, exp(lambda[1+y[A,B]] + bWG*W[A] + bWR*W[B]))
giftsBA[i] = rpois(1, exp(lambda[1+y[B,A]] + bWG*W[B] + bWR*W[A]))
}
sim_data = list(
GAB = giftsAB,
GBA = giftsBA,
N_dyads = N_dyads,
D = 1:N_dyads,
N_households = N,
HA = 1:N,
HB = 1:N
)
f_general = alist(
GAB ~ poisson(lambdaAB),
GBA ~ poisson(lambdaBA),
log(lambdaAB) <- a + T[D,1] + gr[HA,1] + gr[HB, 2],
log(lambdaBA) <- a + T[D,2] + gr[HB,1] + gr[HA, 2],
a ~ normal(0,1),
transpars> matrix[N_dyads, 2]:T <-
compose_noncentered(rep_vector(sigma_T,2), L_Rho_T,Z),
matrix[2, N_dyads]:Z ~ normal(0,1),
cholesky_factor_corr[2]:L_Rho_T ~ lkj_corr_cholesky(2),
sigma_T ~ exponential(1),
transpars> matrix[N_households, 2]:gr <-
compose_noncentered(sigma_gr, L_Rho_gr, Zgr),
matrix[2, N_households]: Zgr ~ normal(0,1),
cholesky_factor_corr[2]: L_Rho_gr ~ lkj_corr_cholesky(2),
vector[2]: sigma_gr ~ exponential(1),
gq>matrix[2,2]:Rho_T <<- Chol_to_Corr(L_Rho_T),
gq>matrix[2,2]:Rho_gr <<- Chol_to_Corr(L_Rho_gr)
)
mGDGR = ulam(f_genera, data = sim_data, chains = 4, cores = 4, iter = 2000)
N = 25
dyads = t(combn(N,2))
N_dyads = nrow(dyads)
f = rbern(N_dyads, 0.1)
alpha = (-3)
y = matrix(NA, N, N)
for(i in 1:N) for(j in 1:N){
if(i!=j){
ids = sort(c(i,j))
the_dyads = which(dyads[,1] == ids[1] & dyads[,2] == ids[2])
p_tie = f[the_dyads] + (1-f[the_dyads]) * inv_logit(alpha)
y[i,j] = rbern(1, p_tie)
}
}
#Simuate gifts
giftsAB = rep(NA, N_dyads)
giftsBA = rep(NA, N_dyads)
lambda = log(c(0.5, 2))
for(i in 1:N_dyads){
A = dyads[i,1]
B = dyads[i,2]
giftsAB[i] = rpois(1, exp(lambda[1 + y[A,B]]))
giftsBA[i] = rpois(1, exp(lambda[1 + y[B,A]]))
}
sim_data = list(
GAB = giftsAB,
GBA = giftsBA,
N_dyads = N_dyads,
D = 1:N_dyads
)
# library(igraph)
# sng = graph_from_adjacency_matrix(y)
# lx = layout_nicely(sng)
# vcol = "#DE536B"
# plot(sng, layout = lx, vertex.size = 8, edge.arrow.size = 0.75, edge.width = 2,
#      edge.curved = 0.35, vertex.color = vcol, edge.color = grau(), asp = 0.9,
#      margin = -0.05, vertex.label = NA)
#
#
# f_dyad = alist(
#   GAB ~ poisson(lambdaAB),
#   GBA ~ poisson(lambdaBA),
#   log(lambdaAB) <- a + T[D,1],
#   log(lambdaBA) <- a + T[D,2],
#   a ~ normal(0,1),
#
#   transpars> matrix[N_dyads, 2]:T <-
#     compose_noncentered(rep_vector(sigma_T,2), L_Rho_T,Z),
#   matrix[2, N_dyads]:Z ~ normal(0,1),
#   cholesky_factor_corr[2]:L_Rho_T ~ lkj_corr_cholesky(2),
#   sigma_T ~ exponential(1),
#
#   gq>matrix[2,2]:Rho_T <<- Chol_to_Corr(L_Rho_T)
# )
#
# mGD = ulam(f_dyad, data = sim_data, chains = 4, cores = 4, iter = 2000)
# precis(mGD, 3, pars = "T")
data(KosterLeckie)
kl_data = list(
N_dyads = nrow(kl_dyads),
N_househlds = max(kl_dyads$hidB),
D = 1:nrow(kl_dyads),
HA = kl_dyads$hidA,
HB = kl_dyads$hidB,
GAB = kl_dyads$giftsAB,
GBA = kl_dyads$giftsBA
)
mGDkl = ulam(f_dyad, data = kl_data, chains = 4, cores = 4, iter = 2000)
precis(mGDkl, depth = 3, pars = c("a", "Rho_T", "sigma_T"))
#Simulate wealth variable
W = rnorm(N)
bWG = 0.5
bWR = (-1)
giftsAB = rep(NA, N_dyads)
giftsBA = rep(NA, N_dyads)
lambda = log(c(0.5, 2))
for(i in 1:N_dyads){
A = dyads[i, 1]
B = dyads[i, 2]
giftsAB[i] = rpois(1, exp(lambda[1+y[A,B]] + bWG*W[A] + bWR*W[B]))
giftsBA[i] = rpois(1, exp(lambda[1+y[B,A]] + bWG*W[B] + bWR*W[A]))
}
sim_data = list(
GAB = giftsAB,
GBA = giftsBA,
N_dyads = N_dyads,
D = 1:N_dyads,
N_households = N,
HA = 1:N,
HB = 1:N
)
f_general = alist(
GAB ~ poisson(lambdaAB),
GBA ~ poisson(lambdaBA),
log(lambdaAB) <- a + T[D,1] + gr[HA,1] + gr[HB, 2],
log(lambdaBA) <- a + T[D,2] + gr[HB,1] + gr[HA, 2],
a ~ normal(0,1),
transpars> matrix[N_dyads, 2]:T <-
compose_noncentered(rep_vector(sigma_T,2), L_Rho_T,Z),
matrix[2, N_dyads]:Z ~ normal(0,1),
cholesky_factor_corr[2]:L_Rho_T ~ lkj_corr_cholesky(2),
sigma_T ~ exponential(1),
transpars> matrix[N_households, 2]:gr <-
compose_noncentered(sigma_gr, L_Rho_gr, Zgr),
matrix[2, N_households]: Zgr ~ normal(0,1),
cholesky_factor_corr[2]: L_Rho_gr ~ lkj_corr_cholesky(2),
vector[2]: sigma_gr ~ exponential(1),
gq>matrix[2,2]:Rho_T <<- Chol_to_Corr(L_Rho_T),
gq>matrix[2,2]:Rho_gr <<- Chol_to_Corr(L_Rho_gr)
)
mGDGR = ulam(f_general, data = sim_data, chains = 4, cores = 4, iter = 2000)
stancode(f_general)
dayds
dyads
sim_data = list(
GAB = giftsAB,
GBA = giftsBA,
N_dyads = N_dyads,
D = 1:N_dyads,
N_households = N,
HA = dyads[,1],
HB = dyads[,2]
)
f_general = alist(
GAB ~ poisson(lambdaAB),
GBA ~ poisson(lambdaBA),
log(lambdaAB) <- a + T[D,1] + gr[HA,1] + gr[HB, 2],
log(lambdaBA) <- a + T[D,2] + gr[HB,1] + gr[HA, 2],
a ~ normal(0,1),
transpars> matrix[N_dyads, 2]:T <-
compose_noncentered(rep_vector(sigma_T,2), L_Rho_T,Z),
matrix[2, N_dyads]:Z ~ normal(0,1),
cholesky_factor_corr[2]:L_Rho_T ~ lkj_corr_cholesky(2),
sigma_T ~ exponential(1),
transpars> matrix[N_households, 2]:gr <-
compose_noncentered(sigma_gr, L_Rho_gr, Zgr),
matrix[2, N_households]: Zgr ~ normal(0,1),
cholesky_factor_corr[2]: L_Rho_gr ~ lkj_corr_cholesky(2),
vector[2]: sigma_gr ~ exponential(1),
gq>matrix[2,2]:Rho_T <<- Chol_to_Corr(L_Rho_T),
gq>matrix[2,2]:Rho_gr <<- Chol_to_Corr(L_Rho_gr)
)
mGDGR = ulam(f_general, data = sim_data, chains = 4, cores = 4, iter = 2000)
precis(mGDGR)
precis(mGDGR, depth = 2)
f_general = alist(
GAB ~ poisson(lambdaAB),
GBA ~ poisson(lambdaBA),
log(lambdaAB) <- a + T[D,1] + gr[HA,1] + gr[HB, 2],
log(lambdaBA) <- a + T[D,2] + gr[HB,1] + gr[HA, 2],
a ~ normal(0,1),
transpars> matrix[N_dyads, 2]:T <-
compose_noncentered(rep_vector(sigma_T,2), L_Rho_T,Z),
matrix[2, N_dyads]:Z ~ normal(0,1),
cholesky_factor_corr[2]:L_Rho_T ~ lkj_corr_cholesky(2),
sigma_T ~ exponential(1),
transpars> matrix[N_households, 2]:gr <-
compose_noncentered(sigma_gr, L_Rho_gr, Zgr),
matrix[2, N_households]: Zgr ~ normal(0,1),
cholesky_factor_corr[2]: L_Rho_gr ~ lkj_corr_cholesky(2),
vector[2]: sigma_gr ~ exponential(1),
gq>matrix[2,2]:Rho_T <<- Chol_to_Corr(L_Rho_T),
gq>matrix[2,2]:Rho_gr <<- Chol_to_Corr(L_Rho_gr)
)
mGDGR = ulam(f_general, data = kl_data, chains = 4, cores = 4, iter = 2000)
N = 25
dyads = t(combn(N,2))
N_dyads = nrow(dyads)
f = rbern(N_dyads, 0.1)
alpha = (-3)
y = matrix(NA, N, N)
for(i in 1:N) for(j in 1:N){
if(i!=j){
ids = sort(c(i,j))
the_dyads = which(dyads[,1] == ids[1] & dyads[,2] == ids[2])
p_tie = f[the_dyads] + (1-f[the_dyads]) * inv_logit(alpha)
y[i,j] = rbern(1, p_tie)
}
}
#Simuate gifts
giftsAB = rep(NA, N_dyads)
giftsBA = rep(NA, N_dyads)
lambda = log(c(0.5, 2))
for(i in 1:N_dyads){
A = dyads[i,1]
B = dyads[i,2]
giftsAB[i] = rpois(1, exp(lambda[1 + y[A,B]]))
giftsBA[i] = rpois(1, exp(lambda[1 + y[B,A]]))
}
sim_data = list(
GAB = giftsAB,
GBA = giftsBA,
N_dyads = N_dyads,
D = 1:N_dyads
)
# library(igraph)
# sng = graph_from_adjacency_matrix(y)
# lx = layout_nicely(sng)
# vcol = "#DE536B"
# plot(sng, layout = lx, vertex.size = 8, edge.arrow.size = 0.75, edge.width = 2,
#      edge.curved = 0.35, vertex.color = vcol, edge.color = grau(), asp = 0.9,
#      margin = -0.05, vertex.label = NA)
#
#
# f_dyad = alist(
#   GAB ~ poisson(lambdaAB),
#   GBA ~ poisson(lambdaBA),
#   log(lambdaAB) <- a + T[D,1],
#   log(lambdaBA) <- a + T[D,2],
#   a ~ normal(0,1),
#
#   transpars> matrix[N_dyads, 2]:T <-
#     compose_noncentered(rep_vector(sigma_T,2), L_Rho_T,Z),
#   matrix[2, N_dyads]:Z ~ normal(0,1),
#   cholesky_factor_corr[2]:L_Rho_T ~ lkj_corr_cholesky(2),
#   sigma_T ~ exponential(1),
#
#   gq>matrix[2,2]:Rho_T <<- Chol_to_Corr(L_Rho_T)
# )
#
# mGD = ulam(f_dyad, data = sim_data, chains = 4, cores = 4, iter = 2000)
# precis(mGD, 3, pars = "T")
data(KosterLeckie)
kl_data = list(
N_dyads = nrow(kl_dyads),
N_households = max(kl_dyads$hidB),
D = 1:nrow(kl_dyads),
HA = kl_dyads$hidA,
HB = kl_dyads$hidB,
GAB = kl_dyads$giftsAB,
GBA = kl_dyads$giftsBA
)
# mGDkl = ulam(f_dyad, data = kl_data, chains = 4, cores = 4, iter = 2000)
#
# precis(mGDkl, depth = 3, pars = c("a", "Rho_T", "sigma_T"))
#Simulate wealth variable
W = rnorm(N)
bWG = 0.5
bWR = (-1)
giftsAB = rep(NA, N_dyads)
giftsBA = rep(NA, N_dyads)
lambda = log(c(0.5, 2))
for(i in 1:N_dyads){
A = dyads[i, 1]
B = dyads[i, 2]
giftsAB[i] = rpois(1, exp(lambda[1+y[A,B]] + bWG*W[A] + bWR*W[B]))
giftsBA[i] = rpois(1, exp(lambda[1+y[B,A]] + bWG*W[B] + bWR*W[A]))
}
sim_data = list(
GAB = giftsAB,
GBA = giftsBA,
N_dyads = N_dyads,
D = 1:N_dyads,
N_households = N,
HA = dyads[,1],
HB = dyads[,2]
)
f_general = alist(
GAB ~ poisson(lambdaAB),
GBA ~ poisson(lambdaBA),
log(lambdaAB) <- a + T[D,1] + gr[HA,1] + gr[HB, 2],
log(lambdaBA) <- a + T[D,2] + gr[HB,1] + gr[HA, 2],
a ~ normal(0,1),
transpars> matrix[N_dyads, 2]:T <-
compose_noncentered(rep_vector(sigma_T,2), L_Rho_T,Z),
matrix[2, N_dyads]:Z ~ normal(0,1),
cholesky_factor_corr[2]:L_Rho_T ~ lkj_corr_cholesky(2),
sigma_T ~ exponential(1),
transpars> matrix[N_households, 2]:gr <-
compose_noncentered(sigma_gr, L_Rho_gr, Zgr),
matrix[2, N_households]: Zgr ~ normal(0,1),
cholesky_factor_corr[2]: L_Rho_gr ~ lkj_corr_cholesky(2),
vector[2]: sigma_gr ~ exponential(1),
gq>matrix[2,2]:Rho_T <<- Chol_to_Corr(L_Rho_T),
gq>matrix[2,2]:Rho_gr <<- Chol_to_Corr(L_Rho_gr)
)
mGDGR = ulam(f_general, data = kl_data, chains = 4, cores = 4, iter = 2000)
precis(mGDGR, depth = 2)
library(rstan)
library(tidyverse)
library(ggplot2)
library(bayesplot)
options(scipen=999)
options(mc.cores = parallel::detectCores())
base_dir = getwd()
model_code_dir = file.path(base_dir, "Model Code")
data_dir = file.path(base_dir, "Processed Data No Zero")
output_dir = file.path(base_dir, "Model Results")
suffix = "_B_1.rds"
data_file = paste0("DataSet", suffix)
data_read = readRDS(file.path(data_dir, data_file))
library(tidyverse)
library(rstan)
library(ggplot2)
Sys.setenv(RSTUDIO = 1)
options(scipen=999)
options(mc.cores = parallel::detectCores())
base_dir = getwd()
model_code_dir = base_dir
model_input_dir = file.path(base_dir, "Model Inputs")
output_dir = file.path(base_dir, "Model Outputs")
data_filename = "cohort_input_no_min_cases.rds"
input_data_path = file.path(model_input_dir, data_filename)
input_data = readRDS(input_data_path)
setwd("C:/Users/hadri/OneDrive - Imperial College London/PhD/Literature Review/Post-ESA Data Extraction/Extraction/Group Review/Jan Update Analysis/Cohort Analysis")
library(tidyverse)
library(rstan)
library(ggplot2)
Sys.setenv(RSTUDIO = 1)
options(scipen=999)
options(mc.cores = parallel::detectCores())
base_dir = getwd()
model_code_dir = base_dir
model_input_dir = file.path(base_dir, "Model Inputs")
output_dir = file.path(base_dir, "Model Outputs")
data_filename = "cohort_input_no_min_cases.rds"
input_data_path = file.path(model_input_dir, data_filename)
input_data = readRDS(input_data_path)
data_list = input_data$data_list
data_list$coeff_prior_mean = 0
data_list$coeff_prior_sd = 2
data_list$sd_prior_mean = 0.5
data_list$sd_prior_sd = 2
model = stan_model(file.path(model_code_dir, "CohortLogisticRegression_FixedEffects.stan"))
fit_model = sampling(model, data = data_list, iter = 10000, chains = 4, seed = 0,
control = list(max_treedepth = 12, adapt_delta = 0.99))
saveRDS(fit_model, file.path(output_dir, paste0("FE_Results_LogisticRegression_mean=", data_list$coeff_prior_mean,"_sd=", data_list$coeff_prior_sd, "_sd_mean=",  data_list$sd_prior_mean, "_sdsd=", data_list$sd_prior_sd, "_min_cases=1.rds")))
summ = summary(fit_model)$summary
library(tidyverse)
library(rstan)
library(ggplot2)
Sys.setenv(RSTUDIO = 1)
options(scipen=999)
options(mc.cores = parallel::detectCores())
base_dir = getwd()
model_code_dir = base_dir
model_input_dir = file.path(base_dir, "Model Inputs")
output_dir = file.path(base_dir, "Model Outputs")
data_filename = "cohort_input_no_min_cases.rds"
input_data_path = file.path(model_input_dir, data_filename)
input_data = readRDS(input_data_path)
data_list = input_data$data_list
data_list$coeff_prior_mean = 0
data_list$coeff_prior_sd = 2
data_list$sd_prior_mean = 0.5
data_list$sd_prior_sd = 2
model = stan_model(file.path(model_code_dir, "CohortLogisticRegression_FixedEffects.stan"))
fit_model = sampling(model, data = data_list, iter = 10000, chains = 4, seed = 0,
control = list(max_treedepth = 12, adapt_delta = 0.99))
saveRDS(fit_model, file.path(output_dir, paste0("FE_Results_LogisticRegression_mean=", data_list$coeff_prior_mean,"_sd=", data_list$coeff_prior_sd, "_sd_mean=",  data_list$sd_prior_mean, "_sdsd=", data_list$sd_prior_sd, "_min_cases=1.rds")))
summ = summary(fit_model)$summary
setwd("C:/Users/hadri/OneDrive - Imperial College London/PhD/Literature Review/Post-ESA Data Extraction/Extraction/Group Review/GitHubRepo/Analysis/Cohort Analysis")
library(tidyverse)
library(rstan)
library(ggplot2)
Sys.setenv(RSTUDIO = 1)
options(scipen=999)
options(mc.cores = parallel::detectCores())
base_dir = getwd()
model_code_dir = base_dir
model_input_dir = file.path(base_dir, "Model Inputs")
output_dir = file.path(base_dir, "Model Outputs")
data_filename = "cohort_input_no_min_cases.rds"
input_data_path = file.path(model_input_dir, data_filename)
input_data = readRDS(input_data_path)
data_list = input_data$data_list
data_list$coeff_prior_mean = 0
data_list$coeff_prior_sd = 2
data_list$sd_prior_mean = 0.5
data_list$sd_prior_sd = 2
model = stan_model(file.path(model_code_dir, "CohortLogisticRegression_FixedEffects.stan"))
fit_model = sampling(model, data = data_list, iter = 10000, chains = 4, seed = 0,
control = list(max_treedepth = 12, adapt_delta = 0.99))
saveRDS(fit_model, file.path(output_dir, paste0("FE_Results_LogisticRegression_mean=", data_list$coeff_prior_mean,"_sd=", data_list$coeff_prior_sd, "_sd_mean=",  data_list$sd_prior_mean, "_sdsd=", data_list$sd_prior_sd, "_min_cases=1.rds")))
summ = summary(fit_model)$summary
library(tidyverse)
library(rstan)
library(ggplot2)
Sys.setenv(RSTUDIO = 1)
options(scipen=999)
options(mc.cores = parallel::detectCores())
base_dir = getwd()
model_code_dir = base_dir
io_set = "Main Results"
model_input_dir = file.path(base_dir, "Model Inputs")
output_dir = file.path(base_dir, io_set, "Model Outputs")
data_filename = "cohort_input_no_min_cases.rds"
input_data_path = file.path(model_input_dir, data_filename)
input_data = readRDS(input_data_path)
data_list = input_data$data_list
data_list$coeff_prior_mean = 0
data_list$coeff_prior_sd = 2
data_list$sd_prior_mean = 0.5
data_list$sd_prior_sd = 2
model = stan_model(file.path(model_code_dir, "CohortLogisticRegression_FixedEffects.stan"))
fit_model = sampling(model, data = data_list, iter = 10000, chains = 4, seed = 0,
control = list(max_treedepth = 12, adapt_delta = 0.99))
library(tidyverse)
library(rstan)
library(ggplot2)
Sys.setenv(RSTUDIO = 1)
options(scipen=999)
options(mc.cores = parallel::detectCores())
base_dir = getwd()
model_code_dir = base_dir
model_input_dir = file.path(base_dir, "Model Inputs")
output_dir = file.path(base_dir, "Model Outputs")
data_filename = "cohort_input_no_min_cases.rds"
input_data_path = file.path(model_input_dir, data_filename)
input_data = readRDS(input_data_path)
data_list = input_data$data_list
data_list$coeff_prior_mean = 0
data_list$coeff_prior_sd = 2
data_list$sd_prior_mean = 0.5
data_list$sd_prior_sd = 2
model = stan_model(file.path(model_code_dir, "CohortLogisticRegression_FixedEffects.stan"))
fit_model = sampling(model, data = data_list, iter = 10000, chains = 4, seed = 0,
control = list(max_treedepth = 12, adapt_delta = 0.99))
saveRDS(fit_model, file.path(output_dir, paste0("FE_Results_LogisticRegression_mean=", data_list$coeff_prior_mean,"_sd=", data_list$coeff_prior_sd, "_sd_mean=",  data_list$sd_prior_mean, "_sdsd=", data_list$sd_prior_sd, "_min_cases=1.rds")))
summ = summary(fit_model)$summary
library(tidyverse)
library(rstan)
library(ggplot2)
Sys.setenv(RSTUDIO = 1)
options(scipen=999)
options(mc.cores = parallel::detectCores())
base_dir = getwd()
model_code_dir = base_dir
model_input_dir = file.path(base_dir, "Model Inputs")
output_dir = file.path(base_dir, "Model Outputs")
data_filename = "cohort_input_no_min_cases.rds"
input_data_path = file.path(model_input_dir, data_filename)
input_data = readRDS(input_data_path)
data_list = input_data$data_list
data_list$coeff_prior_mean = 0
data_list$coeff_prior_sd = 2
data_list$sd_prior_mean = 0.5
data_list$sd_prior_sd = 2
model = stan_model(file.path(model_code_dir, "CohortLogisticRegression_FE.stan"))
fit_model = sampling(model, data = data_list, iter = 10000, chains = 4, seed = 0,
control = list(max_treedepth = 12, adapt_delta = 0.99))
saveRDS(fit_model, file.path(output_dir, paste0("FE_Results_LogisticRegression_mean=", data_list$coeff_prior_mean,"_sd=", data_list$coeff_prior_sd, "_sd_mean=",  data_list$sd_prior_mean, "_sdsd=", data_list$sd_prior_sd, "_min_cases=1.rds")))
summ = summary(fit_model)$summary
setwd("C:/Users/hadri/OneDrive - Imperial College London/PhD/Literature Review/Post-ESA Data Extraction/Extraction/Group Review/GitHubRepo/Analysis/Cohort Analysis")
library(tidyverse)
library(rstan)
library(ggplot2)
Sys.setenv(RSTUDIO = 1)
options(scipen=999)
options(mc.cores = parallel::detectCores())
base_dir = getwd()
model_code_dir = base_dir
model_input_dir = file.path(base_dir, "Model Inputs")
output_dir = file.path(base_dir, "Model Outputs")
data_filename = "cohort_input_no_min_cases.rds"
input_data_path = file.path(model_input_dir, data_filename)
input_data = readRDS(input_data_path)
data_list = input_data$data_list
data_list$coeff_prior_mean = 0
data_list$coeff_prior_sd = 2
data_list$sd_prior_mean = 0.5
data_list$sd_prior_sd = 2
model = stan_model(file.path(model_code_dir, "CohortLogisticRegression_FE.stan"))
fit_model = sampling(model, data = data_list, iter = 10000, chains = 4, seed = 0,
control = list(max_treedepth = 12, adapt_delta = 0.99))
saveRDS(fit_model, file.path(output_dir, paste0("FE_Results_LogisticRegression_mean=", data_list$coeff_prior_mean,"_sd=", data_list$coeff_prior_sd, "_sd_mean=",  data_list$sd_prior_mean, "_sdsd=", data_list$sd_prior_sd, "_min_cases=1.rds")))
summ = summary(fit_model)$summary
